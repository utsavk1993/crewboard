{
  "name": "crewboard",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api",
    "db:seed:large": "npm run db:seed:large -w apps/api --",
    "db:reset": "npm run db:reset -w apps/api",
    "setup": "npm run db:up && npm run prisma:generate -w apps/api && npm run db:migrate && npm run db:seed",
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/web",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "allowScripts": {
    "@prisma/engines@7.10.0": true,
    "prisma@7.10.0": true,
    "esbuild@0.28.2": true,
    "@swc/core@1.16.2": true,
    "unrs-resolver@1.12.2": true
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
